# Tide — Pending Work

- [ ] Run the Tauri app locally and review the hero composition, waterline crop, video playback, and responsive sizing. (The workspace sandbox blocks localhost, so this must be done on the desktop environment.)
- [ ] Record portfolio clips: launch, mouse ripple, quick-add, sync rise animation, and preview-device fill changes.
- [x] Use Bricolage Grotesque for the Tide wordmark and DM Sans for the interface.
- [ ] Add the desired surface-to-underwater entrance clip when it is available.
- [ ] Rename the project folder to `tide` when ready; decide separately whether package, Cargo, GitHub repository, and identifier names should also change.
- [ ] Rotate the GitHub credential that was previously embedded in the Git remote URL, then change the remote to a credential-free URL.
